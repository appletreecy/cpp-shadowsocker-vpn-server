name: Build & Deploy Shadowsocks Server

on:
  push:
    branches: [ "main" ]  # change if your main branch is different

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}:latest

  deploy-to-linode:
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
      - name: Configure SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.LINODE_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Add Linode host to known_hosts
        run: |
          ssh-keyscan -H ${{ secrets.LINODE_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy on Linode via SSH
        env:
          LINODE_HOST: ${{ secrets.LINODE_HOST }}
          LINODE_SSH_USER: ${{ secrets.LINODE_SSH_USER }}   # set to 'root' in your secrets
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
          GITHUB_OWNER: ${{ github.repository_owner }}
          SS_PASSWORD: ${{ secrets.SS_PASSWORD }}
        run: |
          ssh "${LINODE_SSH_USER}@${LINODE_HOST}" << EOF
          set -e

          cd /root/vpn-stack

          # Login to GHCR on Linode
          echo "${GHCR_TOKEN}" | docker login ghcr.io -u "${GITHUB_OWNER}" --password-stdin || true

          # Export Shadowsocks password for docker-compose env
          export SS_PASSWORD="${SS_PASSWORD}"

          # Pull latest image & restart
          docker compose pull
          docker compose up -d
          EOF
